export default class Statistics {
    constructor (id_user, number_visits, time_in_game, count_points){
        this.id_user = id_user
        this.number_visits = number_visits
        this.time_in_game = time_in_game
        this.count_points = count_points
    }
}