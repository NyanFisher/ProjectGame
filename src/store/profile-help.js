export default class Profile{
    constructor(user_id, nickname, avatar, first_name, second_name){
        this.user_id = user_id
        this.nickname = nickname
        this.first_name = first_name
        this.second_name = second_name
        this.avatar = avatar
    }
}